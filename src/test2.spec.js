const assert = require('assert');

describe('super test', () => {
  describe('by some condition', () => {
    it('works properly', () => {
      console.log('hello1');
      assert.equal(2, 1) 
    });
  })
});
